// Para personalizar se le enviarán los estilos desde fuera, por lo tanto recibirá la propiedad className
function Logo({ className }) {
    return <span className={className}>Kewallet</span>;
}

export default Logo;
